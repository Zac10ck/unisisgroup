# Unisis Group website

Official static website for Unisis Group. The site is published from the `main` branch with GitHub Pages.

## Local preview

Serve the repository root with any static web server, then open `index.html` through that server.
